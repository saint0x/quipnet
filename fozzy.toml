base_dir = ".fozzy"

[package]
name = "quicnet-local"
version = "0.1.0"

[[target.bin]]
name = "delivery-probe"
path = "tests/fozzy/src/main.fzy"

[unsafe]
contracts = "compiler"
enforce_dev = false
enforce_verify = true
enforce_release = true
deny_unsafe_in = []
allow_unsafe_in = []
