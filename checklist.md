- [ ] Finish the runtime session state machine from `docs/runtime-lifecycle.md` by covering the remaining degraded/failed/closed/stale semantics end to end and exposing clearer operator-visible transition reasons.
- [ ] Finish the remaining authority-driven runtime reevaluation gaps from `docs/authority-reevaluation.md`: the remaining path-candidate/runtime-truth edge cases and any richer listener lifecycle handling beyond the now-implemented listener suppression visibility plus sync-triggered close, suppress, degrade, and migrate behavior under partial authority change.
- [ ] Finish the daemon-owned path and reconnect model from `docs/path-and-reconnect.md` by building on the now-daemon-backed runtime path inspection surface, reconnect-attempt ownership, bounded retry/backoff, suppression clearing behavior, relay fallback diagnostics, and failed-reconnect path explanations with any remaining richer operator-visible path-choice and runtime-transition detail.
- [ ] Eliminate the remaining non-session cases where cached local state is still presented as live runtime truth, especially for richer authority-driven runtime status transitions beyond the now-daemon-backed runtime health and reconnect surface.
- [ ] Finish the durable-state contract for `~/.quip/net/state.json` by adding explicit migration handling beyond the now-aligned daemon-backed `state show|validate|reset` surface and refreshed `schemas/state/` plus `fixtures/state/`.
- [ ] Finish wiring `schemas/daemon/` and `fixtures/daemon/` into daemon API integration tests and CLI rendering tests beyond the now-added contract checks for runtime listeners, runtime paths, runtime events, runtime health, authority show, identity show/verify, and durable-state `state show|validate|reset` fixtures.
- [ ] Finish the bounded runtime event surface from `docs/runtime-events.md` by enforcing the remaining session failure/closure and path-failure transitions through daemon API integration tests and Fozzy scenario assertions beyond the now-covered reconnect progress, reconnect failure, suppression clearing, path migration completion, and authority reevaluation outcomes.
- [ ] Finish backing the documented operator workflows with real daemon/API commands, especially runtime-backed authority sync effects, revocation and grant changes that drive runtime action, fuller recovery diagnostics, and the remaining runtime path and listener inspection workflows beyond the now-implemented daemon-backed authority, identity, and durable-state inspection/reset surface.
- [ ] Finish the remaining repo-wide `quip` rename fallout in tests, scripts, release assets, packaging surfaces, protocol identifiers, and stale local source references beyond the now-updated release artifact names, Terraform project default, and Fozzy probe label.
- [ ] Expand the production verification program from `docs/verification-plan.md` beyond the now-validated local smoke, restart smoke, reconnect smoke, authority reevaluation, backup/restore, and relay-or-migration flows so the broader runtime model is covered with Fozzy: deterministic scenarios, recorded traces, replay verification, CI-style trace validation, and host-backed runs where feasible.
- [ ] Execute the remaining machine-readable Fozzy scenario catalog in `verification/fozzy/scenarios.json`, the rest of the required trace set in `verification/fozzy/traces.json`, and the full release contract in `verification/fozzy/release-gate.json` beyond the now-exercised local smoke, restart smoke, reconnect smoke, authority reevaluation, backup/restore, and relay-or-migration scenarios plus authority, backup/restore, and relay-or-migration trace recording.
- [ ] Enforce `docs/production-readiness.md` and `docs/release-checklist.md` as the real ship gate with no waived runtime-ownership, durable-state, authority, path, or verification failures.
