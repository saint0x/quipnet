- [ ] Finish making `quipd` the single owner of live runtime truth end to end by adding the remaining daemon-owned active-path registry, daemon-owned reconnect controller, and restart-safe runtime ownership rules beyond session persistence.
- [ ] Implement the runtime session state machine from `docs/runtime-lifecycle.md`: explicit states, explicit transitions, explicit closure reasons, daemon-owned reconcile, and daemon-owned upgrade semantics.
- [ ] Implement authority-driven runtime reevaluation from `docs/authority-reevaluation.md`: membership loss handling, capability-change handling, revocation-driven closure, and reconnect suppression after policy denial.
- [ ] Implement the daemon-owned path and reconnect model from `docs/path-and-reconnect.md`: active-path ownership, relay fallback, path migration, bounded retry/backoff, suppression rules, and operator-visible path-choice explanations.
- [ ] Implement the concrete daemon control surface from `docs/daemon-api-schema.md` and `docs/daemon-api-examples.md`: stable request/response envelopes, stable error codes, runtime inspection operations, session lifecycle operations, authority operations, identity operations, and durable-state operations.
- [ ] Finish aligning the real `quip` command tree to `docs/cli-surface.md`, especially the missing daemon-backed runtime/session surface and the remaining authority/runtime distinctions beyond the now-implemented `authority`, `identity`, and `state` command groups.
- [ ] Eliminate the remaining non-session cases where cached local state is still presented as live runtime truth, especially around path, reconnect, and authority-driven runtime status.
- [ ] Finish the durable-state contract for `~/.quip/net/state.json` by adding explicit migration handling and bringing the in-code validator fully into line with `schemas/state/` and `fixtures/state/`.
- [ ] Wire `schemas/daemon/` and `fixtures/daemon/` into daemon API integration tests and CLI rendering tests.
- [ ] Wire `schemas/events/runtime-event.schema.json` and `fixtures/events/` into runtime event emission tests and scenario assertions.
- [ ] Implement a bounded runtime event surface matching `docs/runtime-events.md` so lifecycle, authority, path, and reconnect transitions are observable and testable.
- [ ] Finish backing the documented operator workflows with real daemon/API commands, especially runtime-backed authority sync effects, richer authority diagnostics, and fuller recovery diagnostics beyond the now-implemented authority inspection, identity inspection, durable-state validation, and safe state reset surface.
- [ ] Implement safe backup/export and restore support that matches `docs/backup-and-restore.md` and preserves the identity-versus-network-state split.
- [ ] Finish the remaining repo-wide `quip` rename fallout in tests, scripts, release assets, packaging surfaces, and any stale local source references beyond the local Fozzy smoke path already updated.
- [ ] Expand the production verification program from `docs/verification-plan.md` beyond the validated local smoke flow so the broader runtime model is covered with Fozzy: deterministic scenarios, recorded traces, replay verification, CI-style trace validation, and host-backed runs where feasible.
- [ ] Execute the remaining machine-readable Fozzy scenario catalog in `verification/fozzy/scenarios.json`, the rest of the required trace set in `verification/fozzy/traces.json`, and the full release contract in `verification/fozzy/release-gate.json`.
- [ ] Enforce `docs/production-readiness.md` and `docs/release-checklist.md` as the real ship gate with no waived runtime-ownership, durable-state, authority, path, or verification failures.
