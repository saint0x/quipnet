- [ ] Finish the runtime session state machine from `docs/runtime-lifecycle.md` by covering the remaining degraded/failed/closed/stale semantics end to end and exposing clearer operator-visible transition reasons.
- [ ] Finish the remaining authority-driven runtime reevaluation gaps from `docs/authority-reevaluation.md`: listener/path eligibility outcomes beyond the now-implemented sync-triggered, policy-classified close-and-suppress behavior, plus any migrate/degrade cases that should exist under partial authority change.
- [ ] Finish the daemon-owned path and reconnect model from `docs/path-and-reconnect.md` by building on the now-daemon-backed runtime path inspection surface, reconnect-attempt ownership, bounded retry/backoff, and suppression clearing behavior with relay fallback diagnostics and fuller operator-visible path-choice explanations.
- [ ] Finish the concrete daemon control surface from `docs/daemon-api-schema.md` and `docs/daemon-api-examples.md` for the remaining identity verification and durable-state API operations beyond the now-implemented runtime sessions, runtime paths, runtime listeners, runtime health, runtime events, and daemon-backed authority operations.
- [ ] Finish aligning the real `quip` command tree to `docs/cli-surface.md`, especially the remaining daemon-backed surfaces beyond `status`, `runtime sessions`, `runtime listeners`, `runtime paths`, `runtime health`, `runtime events`, session lifecycle commands, and the now-daemon-backed `authority` group.
- [ ] Eliminate the remaining non-session cases where cached local state is still presented as live runtime truth, especially for richer authority-driven runtime status transitions beyond the now-daemon-backed runtime health and reconnect surface.
- [ ] Finish the durable-state contract for `~/.quip/net/state.json` by adding explicit migration handling and bringing the in-code validator fully into line with `schemas/state/` and `fixtures/state/`.
- [ ] Wire `schemas/daemon/` and `fixtures/daemon/` into daemon API integration tests and CLI rendering tests.
- [ ] Wire `schemas/events/runtime-event.schema.json` and `fixtures/events/` into runtime event emission tests and scenario assertions beyond the now-implemented bounded lifecycle/path/suppression/reconnect event buffer and daemon-backed `runtime events` surface.
- [ ] Finish the bounded runtime event surface from `docs/runtime-events.md` by enforcing the now-added authority reevaluation outcomes, reconnect attempt progress, and suppression clearing transitions through daemon API integration tests and Fozzy scenario assertions.
- [ ] Finish backing the documented operator workflows with real daemon/API commands, especially runtime-backed authority sync effects, revocation and grant changes that drive runtime action, fuller recovery diagnostics, runtime path and listener inspection, and daemon-backed authority/identity/state operations beyond the implemented runtime/session surface and durable authority membership/capability/revocation inspection, identity inspection, durable-state validation, and safe state reset surface.
- [ ] Implement safe backup/export and restore support that matches `docs/backup-and-restore.md` and preserves the identity-versus-network-state split.
- [ ] Finish the remaining repo-wide `quip` rename fallout in tests, scripts, release assets, packaging surfaces, and any stale local source references beyond the local Fozzy smoke path already updated.
- [ ] Expand the production verification program from `docs/verification-plan.md` beyond the now-validated local smoke and reconnect smoke flows so the broader runtime model is covered with Fozzy: deterministic scenarios, recorded traces, replay verification, CI-style trace validation, and host-backed runs where feasible.
- [ ] Execute the remaining machine-readable Fozzy scenario catalog in `verification/fozzy/scenarios.json`, the rest of the required trace set in `verification/fozzy/traces.json`, and the full release contract in `verification/fozzy/release-gate.json` beyond the now-exercised local smoke and reconnect smoke scenarios and reconnect trace recording.
- [ ] Enforce `docs/production-readiness.md` and `docs/release-checklist.md` as the real ship gate with no waived runtime-ownership, durable-state, authority, path, or verification failures.
