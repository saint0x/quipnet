# Fixtures

This directory contains concrete example payloads intended for:
- durable-state validation tests
- migration tests
- daemon API contract tests
- CLI rendering tests
- runtime event validation tests
- Fozzy scenario assertions

Fixture groups:
- `state/`
  - valid and invalid durable state examples
- `daemon/`
  - request and response examples for daemon operations
- `events/`
  - runtime event examples
